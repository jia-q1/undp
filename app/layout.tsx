import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Crisis Bureau 2025 — Interactive Report",
  description: "Rapid expert deployment and crisis response across 150+ UNDP country offices. Featuring 2,585 deployments, $98.2M delivered, and 15,000+ vetted experts.",
  keywords: ["Crisis Response", "UNDP", "Workforce", "Deployment", "Crisis Management"],
  authors: [{ name: "UNDP Crisis Bureau" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
